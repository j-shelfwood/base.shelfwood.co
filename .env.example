# InfluxDB connection (set these in Netlify UI — never commit actual values)
INFLUX_URL=https://influx.shelfwood.co
INFLUX_TOKEN=your_admin_token_here
INFLUX_ORG=shelfwood
INFLUX_BUCKET=mc

# TimescaleDB connection
DATABASE_URL=postgres://mctelemetry:password@host:5432/mc_telemetry
